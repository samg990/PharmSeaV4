type Medication
	@model(subscriptions: null)
	@auth(rules: [{ allow: owner }, { allow: private, operations: [read] }]) {
	id: ID!
	name: String!
	brandName: String!
	genericName: String!
	dose: String
	userId: String
}